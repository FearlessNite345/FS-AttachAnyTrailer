config = {}

config.autoAttachHitchToTrailer = false -- If this is true you will auto attach to the trailer once close enough
config.maxDistance = 1.2                -- The max distance a trailer can be attached from
config.attachKeybind = 47               -- Only used if config.autoAttachToTrailer is false

config.locales = {
    ['attachToTrailer'] = 'Attach trailer'
}
